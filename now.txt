BASE

--сделать стили {
    - reboot styles

    - сделать типографиюю стили {


    }

    - typography.css - подкл шрифтов, размеры;

    фрагментирование - можете создавать фрагменты Sass-файла, которые будут содержать в себе небольшие отрывки CSS, которые можно будет использовать в других Sass-файлах.
    сделать отделые блоки Нижнее подчеркивание в имени Sass-файла говорит компилятору о том, что это только фрагмент и он не должен компилироваться в CSS( как отдельный фаил ) sass/_media.sass

    - base.css   -в начале, сброс стандартных отступов, потом базовыве блоки, их надо еще сделть
    - style.css - правка , модификаторы стандартных блоков
    - mediaStyles.css - медиа для мобильных

    -libs.css - слепить стили из подключенных либ, и тогда переносить их в app/css , или
                просто подключать как раньше, только те которые надо.


}


-- основные блоки по верстке
   * - таблицы
    - бредкрамбы
    - флекс итемы для разных количеств столбиков
    - формы
    - картинка (round) + title subtitle (текст справа)
    - картинка (round) + title subtitle (текст снизу)

    - form (name + email + textarea + btn-clear) vertical
    - form (date + date + name + number + bnt-clear) horizontal

    - map(fullwith)
    - video(fullwith)